# テンプレート詳細

このファイルには、開発作業ドキュメント生成時に使用するテンプレートを記載します。
`{要件名}` の部分はユーザーが指定した要件名に置き換えてください。

---

## requirements.md テンプレート

```markdown
# 要件定義書 - {要件名}

## 概要

この開発作業で実現したいことを記載します。

## 背景・目的

なぜこの機能が必要なのか、どのような課題を解決するのかを記載します。

## 要件一覧

### 機能要件

#### F-1: {機能名}

- **説明**:
- **受け入れ条件**:
  - [ ] 条件1
  - [ ] 条件2

#### F-2: {機能名}

- **説明**:
- **受け入れ条件**:
  - [ ] 条件1

### 非機能要件

- **パフォーマンス**:
- **ユーザビリティ**:
- **保守性**:
- **外観・デザイン**: （UI に関係する場合のみ記載。カラーパレットの CSS カスタムプロパティ活用、Radix UI のアクセシビリティ属性など）

## スコープ

### 対象

-

### 対象外

-

## 実装対象ファイル（予定）

- `src/components/...`
- `src/stores/...`
- `src-tauri/src/commands/...`

## 依存関係

-

## 既知の制約

-

## 参考資料

- `docs/steering/features/...`
```

---

## design.md テンプレート

```markdown
# 設計書 - {要件名}

## アーキテクチャ

### 対象コンポーネント

```
Frontend (React/TypeScript)
    ↓ invoke()
Tauri IPC
    ↓
Rust Backend
    ↓
ファイルシステム / PTY
```

### 影響範囲

- **フロントエンド**:
- **バックエンド（Rust）**:

## 実装方針

### 概要

実装の全体的なアプローチを記載します。

### 詳細

1.
2.
3.

## データ構造

### 型定義（TypeScript）

```typescript
interface Example {
  id: string
  name: string
}
```

### 型定義（Rust）

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Example {
    pub id: String,
    pub name: String,
}
```

## API設計

### Tauriコマンド

| コマンド名 | 引数 | 戻り値 | 説明 |
|-----------|------|--------|------|
| `example_command` | `ExampleInput` | `Result<ExampleOutput, String>` | 説明 |

### Tauriイベント

| イベント名 | ペイロード | 説明 |
|-----------|-----------|------|
| `example-event` | `{ id: string }` | 説明 |

## UI設計

### UIライブラリ

（UIコンポーネントを実装する場合のみ記載）

| ライブラリ | 用途 | 備考 |
|-----------|------|------|
| `@radix-ui/react-{コンポーネント名}` | （タブ・ダイアログ・コンテキストメニュー等） | Tailwind v4 と完全互換、アクセシビリティ属性自動付与 |
| `lucide-react` | アイコン | tree-shaking対応 |

### カラーパレット

（UIコンポーネントを実装する場合のみ記載。既存のカラーパレットを使用する）

`src/index.css` で定義済みの CSS カスタムプロパティを使用する：
- `--color-bg-base` / `--color-bg-panel` / `--color-bg-elevated` — 背景色
- `--color-border` — ボーダー・セパレーター
- `--color-text-primary` / `--color-text-muted` — テキスト
- `--color-accent` — アクティブ状態・ハイライト

### 画面構成

-

### コンポーネント構成

-

## 状態管理

### Zustandストア変更

```typescript
// useXxxStore の変更内容
```

## テストコード

### Reactコンポーネントテスト例

```typescript
import { describe, it, expect } from 'vitest'
import { render } from '@testing-library/react'

describe('ExampleComponent', () => {
  it('should render correctly', () => {
    // test implementation
    expect(true).toBe(true)
  })
})
```

### Rustテスト例

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_example() {
        // test implementation
        assert!(true);
    }
}
```

## 設計上の決定事項

| 決定事項 | 理由 | 代替案 |
|---------|------|--------|
| | | |

## 未解決事項

- [ ]
```

---

## tasklist.md テンプレート

```markdown
# タスクリスト - {要件名}

## 進捗サマリー

| 状態 | 件数 |
|------|------|
| 完了 | 0 |
| 進行中 | 0 |
| 未着手 | 0 |

## タスク一覧

### T-1: 要件定義・設計

- [ ] 要件定義書の作成
- [ ] 設計書の作成
- [ ] レビュー完了

### T-2: {タスク名}（バックエンド）

- [ ] Rustコマンド実装 (`src-tauri/src/commands/...`)
- [ ] Rustテスト作成
- [ ] `cargo check` でエラーなし

### T-3: {タスク名}（フロントエンド）

- [ ] コンポーネント実装 (`src/components/...`)
- [ ] ストア更新 (`src/stores/...`)
- [ ] Reactテスト作成

### T-4: 結合・テスト

- [ ] Tauri IPC 疎通確認
- [ ] 手動テスト（testing.md 参照）
- [ ] `npx tauri dev` で動作確認

### T-5: ドキュメント・マージ

- [ ] 永続化ドキュメント更新（必要に応じて）
- [ ] コードレビュー
- [ ] `develop` ブランチへのPRマージ

## 完了条件

- [ ] 全タスクが完了
- [ ] `npm run lint` がエラーなし
- [ ] `cd src-tauri && cargo test` がパス
- [ ] 手動テストが全件OK
- [ ] 永続化ドキュメントが更新済み（必要な場合）
```

---

## task_{タスクID}.md テンプレート

このテンプレートは、タスクの詳細を記載する必要がある場合に使用します。
初期生成では作成せず、開発中に必要に応じて作成してください。

```markdown
# タスク詳細 - T-{タスクID}: {タスク名}

## 概要

このタスクで実現することを簡潔に記載します。

## 関連要件

- F-{要件ID}: {要件名}

## 実装内容

### 対象ファイル

| ファイル | 変更内容 |
|---------|---------|
| `src/components/...` | |
| `src-tauri/src/commands/...` | |

### 実装詳細

#### 1. {実装項目1}

```typescript
// コード例
```

#### 2. {実装項目2}

```rust
// コード例
```

## 技術的な検討事項

### 検討事項1

- **問題**:
- **解決策**:
- **理由**:

## テスト観点

- [ ] {テスト観点1}
- [ ] {テスト観点2}

## 進捗

| 項目 | 状態 | 備考 |
|------|------|------|
| 設計 | 未着手 | |
| 実装 | 未着手 | |
| テスト | 未着手 | |
| レビュー | 未着手 | |
```

---

## testing.md テンプレート

```markdown
# テスト手順書 - {要件名}

## 概要

このドキュメントでは、{要件名}機能のテスト手順を記載します。
可能な限り手動操作で確認し、操作で確認できない項目のみ自動テストを記載します。

## 前提条件

- `npx tauri dev` でアプリが起動していること
- テスト対象のプロジェクトフォルダが準備されていること

## 手動テスト

### ケース 1: {テストケース名}

**手順:**

1. アプリを起動する
2. {画面/操作}を行う
3. {確認事項}を確認する

**期待結果:**

- {期待される動作}

**確認結果:**

- [ ] OK / NG

---

### ケース 2: {テストケース名}

**手順:**

1.
2.
3.

**期待結果:**

-

**確認結果:**

- [ ] OK / NG

---

## 自動テスト

### フロントエンドテスト（Vitest）

```bash
npm run test
```

### 型チェック

```bash
npm run build  # TypeScriptエラーがないことを確認
```

### Rustテスト

```bash
cd src-tauri && cargo test
```

### Rustの型チェック

```bash
cd src-tauri && cargo check
```

## エッジケース

| ケース | 期待動作 | 確認結果 |
|--------|---------|---------|
| | | |

## 回帰テスト

既存機能への影響がないことを確認します。

- [ ] ファイルツリーが正常に表示される
- [ ] ターミナルが正常に起動・動作する
- [ ] コンテンツビューアでファイルが正常に表示される
```
